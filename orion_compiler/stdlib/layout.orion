// Orion Standard Layout Library
// Version 0.1
module layout;

// A component that arranges its children in a vertical column.
// The layout logic is handled by the renderer.
component Column {
    children: [];
    spacing: 0;
}

// A component that arranges its children in a horizontal row.
// The layout logic is handled by the renderer.
component Row {
    children: [];
    spacing: 0;
}
